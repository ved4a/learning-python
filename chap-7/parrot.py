# the input function pauses the program until user input is received
# that input is then stored into a variable which can be used, eg:

message = input("Tell me something, and I'll repeat it back to you: ")
print(message)